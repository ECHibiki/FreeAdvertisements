<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
    </head>
    <body style="margin:0">
<a href="{{ $url }}" target="_blank"><img src="{{ $uri }}" owner="{{ $name }}"/></a>
    </body>
</html>
