<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
    </head>
    <body>
<a href="{{ $url }}"><img src="{{ $uri }}" owner="{{ $name }}"/></a>
    </body>
</html>
